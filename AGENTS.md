# この攻略サイトの制作ルール

- 原本・元メモを変更・改名・削除しない。raw-screenshots/ と article-notes/ と work/ は非公開素材。
- 最小サンプルはマムガール・オーキッド占星術師・紹介記事。残りの素材は未処理でもよい。
- まず README.md と docs/workflow.md、必要時 docs/data-model.md を読む。
- Workが画像を実際に見て分類・読取する。ファイル名だけでミニオンや用途を推測しない。不確実なら work/sources.yaml の status を pending にし処理を保留。
- 詳細画面は通常/ランブルチェスを先に判別する。左上の絆、ステータスの絆ポイント、星4タブをランブルチェスの特徴とする。保証Lv.7未達では現在Lvと保証Lvが併記される。判別手順はdocs/workflow.mdを参照。
- バリアント表示の領域まで確認できた通常詳細画面で表示がなければvariants.statusをabsentにし、分類済み画像のsourcesを付ける。ランブルチェス画面の非表示、切れ・遮蔽・分類不明からは判定しない。
- 絆はカードプールのデッキ見出しと名前を優先して取得し、data/rumble-chess.yamlへ差分保存。通常性能へ混ぜず、現時点ではサイト未使用。ランブルチェス画像と星4画像を必須にせず、星4の未撮影値を規則性から自動補完しない。
- 客観情報は画像から抽出し、data/sources.yaml の根拠IDを付ける。元メモの性能値は画像確認値として使わない。攻略評価・使い方は元メモから清書してよい。
- 攻略本文は書き手自身の意見として記述する。「運営者のメモでは」「運営者の評価では」など、元メモを第三者の発言として紹介する伝聞形式にしない。評価の意味や確度は変えず、未確認事項や客観情報の根拠は引き続き区別する。
- レアリティ最低レベルはレア1、エピック4、レジェンド7（ユーザー確認）。バリアントの育成画面のLvと詳細画面の表示Lvは別に保持。
- observed / manual / derived / unknown / absent を区別。逆算には formula と assumption が必要。未確認を0・なし・推定値に置換しない。
- 基本形・バリアントの性能表は項目ごとにinitialを優先。unknownの項目だけ撮影時のbase値で補い、対象項目・撮影Lv・条件を注記する。initialのobserved/manual/derivedは値のみ表示し、全項目が揃えば撮影条件の注記と別の初期性能表は出さない。攻撃間隔・対象はbaseを使用。initialのabsentは禁止。逆算の式・前提はデータと確認票に保持する。
- 公開記事は人間が確認・編集した攻略情報として届ける。AI・画像認識などの制作方法やmanualの「追記確認」など確認経路のラベルを読者向けに表示しない。status・sourcesは管理用に保持する。未確認、条件付き逆算と前提、撮影レベルなど情報の解釈に必要な条件は表示する。
- data/minions/*.yaml と content/*.md が正本。既存ファイルを先に読み、今回の変更箇所だけ更新する。元メモから全文再生成しない。
- 画像処理は scripts/images.mjs。マスクは原本座標、不透明黒で画素置換。work/processed を目視してから reviewed を true にする。原本やレシピ変更時は false に戻す。
- 画像ファイルの役割は1対多。1原本から複数の画像を生成してよい。原本パスは work/ にだけ記録。
- 公開前に npm test / npm run build。画像追加時は npm run images、加工後画像を確認して npm run images:publish。npm run review で根拠・未確認一覧を確認。
- 自動公開・AI API・常時監視を追加しない。外部公開、commit、pushはユーザーの別途指示が必要。

- ゲーム内表記に合わせ、公開文・管理文書では「バリアント」に統一する。原本・元メモは変更しない。
